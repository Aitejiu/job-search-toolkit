interface:
  display_name: "Resume Registry"
  short_description: "Register private resume versions in Obsidian"
  default_prompt: "Register this resume file in the configured private Obsidian vault, confirm its metadata, and link the confirmed version to the requested application."
